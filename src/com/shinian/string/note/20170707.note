reset();
    故名思议,重置,可以将Matcher对象应用于一个新的字符序列
    不带参数重置到起始位置
    
编程思想的作者在大部分的例子里面都使用了自己封装的类库 而不去想这样到底方便不方便
对于读者来说,我既然看到了String 为什么我要因为一个例子而去跳过中间内容 直接去看后面的部分呢

完整版本的正则匹配注释  :  (?<!:)\\/\\/.*|\\/\\*(\\s|.)*?\\*\\/

匹配所有普通字符串 : [\s\S]*

匹配所有英文 : /[ -~]/ 

匹配所有非英文 : /[^ -~]/



